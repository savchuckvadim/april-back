export enum RegionType {
    REGIONS = '0',
    MSK = '1',
}

export enum SupplyTypeCode {
    INTERNET = '0',
    PROXIMA = '1',
}

export enum SupplyType {
    INTERNET = 'internet',
    PROXIMA = 'proxima',
}
