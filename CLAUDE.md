работаешь тут с проектом nest js
используя Typescript, никогда не используй any
используй документацию nest
если делаешь модуль - пиши покрытие тестами
код тестов и рабочий код не должны сильно смешиваться
соблюдай DRY SOLID KISS
покрывай все места тестами, где могут быть ошибки
Один сервис - одна ответственность
Луковая архитектура
В кононтроллерах и dto пиши описание на русском языке
пиши код аккуратно
используй правила отсюда
C:\Projects\April\april-next\back\ai\rules

если ты используешь правила из данного файла пиши об этом в чате

данный проект обращается к битркикс по апи
для этого есть специальная библиотека
если нужно зачем-то обратиться к битрикс пробуй сделать это через библиотеку
src\modules\bitrix

так как инстанс битрикс может быть использован для разных клиентов по domain

private readonly pbxService: PBXService;

const { bitrix } = await this.pbxService.init(this.domain);

надо следить чтобы в сервисах с @Injactable()
НЕ БЫЛО this.bitrix
иначе может возникнуть race condition и сервис битрикса будет перезатерт
заинжекчен в this может быть только PBXService который по domain отдает инстанс битрикса и портала с ключами доступа и id-шниками

или можно делать не injactable севрвис и вызывать его в целевых сервисах как new SomeService(bitrix)

еще вожно для битрикс инстанса важно как он работает с batch коммандами

апи библиотеки таково
что инстанс битрикса может накапливать batch комманды а потом одним вызовом отправлять их

если ты отправляешь batch комманды из определенного инстанса  необходимо проследить чтоб это был тот же самый инстанс в который ты хотел накопить комманды


если в библиотеки битрикс не хватает каких то методов
ты можешь добавлять их в библиотеку битрикс по MCP документации API битрикс и по документации библиотеки

C:\Projects\April\april-next\back\src\modules\bitrix\BITRIX_DOMAIN_MODULE_GUIDE.md



старайся чтобы классы не разрастались более 200-300(это маааксимум) строк кода
старайся не повторяться и делай переиспользуемую логику

никогда не пуш ничего в db проект с миграциями - другой проект
этот может сделать только pull - но всегда спрашивай если хочешь сделать чтото с db


после изменения или добавления модуля всегда выполняй в модуле комманду "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",

pnpm run lint

если нужно сразу фикси ошибки линтера
