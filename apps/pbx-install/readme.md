расширить модуль до инсталяции-установки определенному клиенту вообще всего что только можно
1) pbx - уже есть. Добавить
    - группы задач
    - поля задач
    - поля пользователя (с помощью полей можно вписывать в пользователя какието values - например план позвонкам, а так же помечать сотрудников определенным признаками и давать доступ на какието действия (например состоит в такойто рассылке или в такойто команде) )
    - id department
    - timezones
    - реквизиты пресеты и поля
    - единицы измерения
    - rpa
    - smarts(протестить)
    - и т.д. нужно уточнить
2) pkt - типа все что относится k portal - konstructor
* есть поля которые как бы для конструктора но они в pbx - потомучто самое важное в pbx
это присутствие записанного в db id из битрикса для того чтобы потом обращаться именнно в битрикс. pkt (konstructor) имеется ввиду без относительно битрикса самого
    - типы договоров
    - шаблон для pdf как
    - регионы,
    - поставщики, налоги, сканы печатей
    - настройки приложения
    - настройки акций
    -
