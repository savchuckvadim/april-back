/**
 * Реализация перенесена в @lib/pbx-user-fields (общая либа: pbx-install +
 * kpi-report-sales/plans). Этот файл — реэкспорт для существующих
 * импортов приложения.
 */
export {
    AbstractBxFieldsInstallService,
    type IBxFieldsInstallResult,
    type IBxInstalledFieldResult,
} from '@lib/pbx-user-fields';
