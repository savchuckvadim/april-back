/** Тип пользовательского поля Bitrix (используется в konstructor и отчётах). */
export enum BitrixFieldType {
    DATE = 'date',
    DATETIME = 'datetime',
    STRING = 'string',
    ENUMERATION = 'enumeration',
}
